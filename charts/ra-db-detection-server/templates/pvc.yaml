kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ra-db-detection-shared-volume
  namespace: test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  volumeName: local-pv-c471c0aa
  storageClassName: myvol
  volumeMode: Block
